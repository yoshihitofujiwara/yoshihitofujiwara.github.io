//--------------------------------------------------------------------------
// grid: グリッド
//--------------------------------------------------------------------------
// vec2 grid(vec2 posi, float num){

    // // Scale
    // posi *= vec2(num.);

    // // Tile the space
    // vec2 i_st = floor(posi);
    // vec2 f_st = fract(posi);


//   return posi;
// }
// #pragma glslify: export(grid)
